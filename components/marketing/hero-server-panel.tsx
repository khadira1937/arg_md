/**
 * Deprecated hosting-era hero visual. Replaced by components/marketing/hero-visual.tsx
 * for ARGANA MEDIA. Kept as an inert stub so any stray import keeps compiling.
 */
export function HeroServerPanel() {
  return null;
}
