/**
 * Deprecated hosting-era preview component. ARGANA MEDIA does not use the
 * server/region dashboard visual on its marketing site. Kept as an inert stub
 * so any stray import keeps compiling; safe to remove once confirmed unused.
 */
export function DashboardPreview() {
  return null;
}
